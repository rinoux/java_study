package com.fr.decision.staticres.utils;

import com.fr.decision.staticres.storage.Storage;

import java.io.InputStream;
import java.io.OutputStream;

/**
 * 操作流的工具, fr的IOUtils包含了资源文件的读取，图片的读取，压缩解压，重命名，文件复制等方法
 * 这里主要还是几种基本的
 *
 */
public class StreamIOUtils {
    public static InputStream getResource(String path, Storage storage) {
        return null;
    }

    public static boolean storeResource(String path, OutputStream outputStream, Storage storage) {
        return false;
    }
}
